# Batch 60 — saved-strategy additions to causal GGG

Tested 8 previously saved robust definitions at 10%, 20%, and 30% allocations (24 blend trials). All candidate rebuilds were deterministic: **True**. All 24 prefix checks passed: **True**.

At 50 bps over the trailing three years, baseline CAGR/Sharpe were `12.31%` / `1.480`. The best point estimate was `blend::candidate-052f66f29b9fafbe::0.3` at `14.89%` / `1.553`, a CAGR change of `+2.58%`. Its 24-trial adjusted paired-bootstrap p-value was `0.0494`.

Blends passing the first screening gates: **3**. Provisional challenger: `blend::candidate-052f66f29b9fafbe::0.3`. Decision: `save_provisional_challenger_for_deeper_validation`. These are retrospective comparisons on already observed history, not guarantees or untouched forward evidence.
